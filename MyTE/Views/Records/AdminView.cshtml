@model IEnumerable<MyTE.Models.BiweeklyRecord>

@{
    ViewData["Title"] = "Relatórios";
}

<h1>@ViewData["Title"]</h1>

<table class="table">
    <thead>
        <tr>
            <th>User</th>
            <th>Periood</th>
            <th>Total Hours</th>
            <th>Detalhes</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var record in Model)
        {
            <tr>
                <td>@record.UserEmail</td>
                <td>@record.StartDate.ToString("dd/MM/yyyy") - @record.EndDate.ToString("dd/MM/yyyy")</td>
                <td>@record.TotalHours</td>
                <td>
                    <button class="btn btn-primary" type="button" data-bs-toggle="collapse" data-bs-target="#details_@record.BiweeklyRecordId" aria-expanded="false" aria-controls="details_@record.BiweeklyRecordId">
                        View Details
                    </button>
                    <div class="collapse" id="details_@record.BiweeklyRecordId">
                        <div class="card card-body">
                            <ul>
                                @foreach (var detail in record.Records)
                                {
                                    <li>@detail.Data.ToString("dd/MM/yyyy"): @detail.Hours hours</li>
                                }
                            </ul>
                        </div>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>
